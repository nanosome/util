// @license@ 
package nanosome.util.normalize {
	import nanosome.util.INormalize;
	
	/**
	 * <code>INormalizeMap</code> is a marker interface to identify <code>
	 * INormalize</code> implementations that just map from a normalized number to
	 * a normalized number. 
	 * 
	 * @author Martin Heidegger
	 * @version 1.0
	 * @see INormalize
	 */
	public interface INormalizeMap extends INormalize {}
}
